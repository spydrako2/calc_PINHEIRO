# Story 1.2: PDF Reader e Detecção Híbrida (Texto vs OCR)

**Status:** Draft
**Story ID:** 1.2
**Epic:** 1 - Core Engine
**Priority:** High
**Complexity:** Medium

## User Story

Como usuário,
eu quero que o sistema leia PDFs de holerites independente de serem texto selecionável ou imagem escaneada,
para que eu possa processar qualquer holerite que eu receba.

## Acceptance Criteria

1. Módulo `pdf_reader.py` implementado que recebe um caminho de PDF e retorna uma lista de páginas com conteúdo extraído
2. Detecção híbrida por página: verifica se a página tem texto extraível (pdfplumber); se o texto extraído for vazio ou abaixo de um limiar mínimo de caracteres, aplica OCR (pytesseract) como fallback
3. Cada página retornada inclui metadados: número da página, método de extração usado (TEXTO ou OCR), e indicador de confiança (0.0-1.0)
4. Funciona com PDFs de até 500 páginas sem memory overflow (processamento página a página, não carregando tudo em memória)
5. Detecção de continuação: identifica páginas 2+ de um holerite (cabeçalho vazio + tabela de verbas). Usa fuzzy matching (score >= 0.75) para template detection com OCR degradado
6. Testes com PDFs reais de referência (pelo menos 1 texto selecionável e 1 escaneado)

## Implementation Plan

### Task 1: Implementar PDFReader com pdfplumber
- [x] Criar `src/core/pdf_reader.py`
- [x] Implementar função `read_pdf(pdf_path)` → lista de páginas
- [x] Cada página: `{numero, texto, metodo, confianca}`
- [x] Usar pdfplumber para extração de texto
- [x] Tests: test_pdf_reader.py com 2+ PDFs reais (9 testes, 78% coverage)

### Task 2: Implementar OCR fallback com pytesseract
- [x] Implementar função `_apply_ocr(page)` → texto
- [x] Detecção de threshold: se len(texto) < 50 chars, aplica OCR
- [x] Confiança OCR = 0.7; Confiança TEXTO = 0.95
- [x] Tests: test_ocr_fallback.py (9 testes)
- [x] Integração no read_pdf() (hybrid extraction)

### Task 3: Implementar detecção de continuação
- [ ] Função `is_continuation_page(texto)` → bool
- [ ] Heurística: cabecalho vazio + tabela de verbas = continuação
- [ ] Tests: test_continuation_detection.py

### Task 4: Integrar fuzzy matching para template detection
- [ ] Usar fuzzywuzzy para detecção com OCR degradado
- [ ] Score >= 0.75 = match válido
- [ ] Tests: test_fuzzy_matching.py

### Task 5: Testes end-to-end
- [ ] Test com PDF DDPE (texto selecionável)
- [ ] Test com PDF escaneado (OCR)
- [ ] Test com PDF 500+ páginas (sem memory overflow)
- [ ] Validar cobertura >= 85%

## Dev Notes

- Use pdfplumber.PDF para acesso iterativo (não carrega tudo em memória)
- pytesseract requer Tesseract OCR instalado (já vem em pyproject.toml via pytesseract)
- Fuzzy matching: `pip install fuzzywuzzy python-Levenshtein` (adicionar ao pyproject.toml)
- Threshold de caracteres: 50 é conservador, pode ajustar conforme testes

## Testing

- Usar PDFs de `docs/referencias/`
- Validar que extraction metadata está correto
- Testar performance com PDF grande (FRANCISCA_LUCIA = 15+ páginas)

## File List

| File | Status | Notes |
|------|--------|-------|
| `src/core/pdf_reader.py` | Done | Main module - PDFReader with pdfplumber |
| `tests/test_pdf_reader.py` | Done | 9 unit tests, 78% coverage |
| `tests/test_ocr_fallback.py` | Pending | OCR tests (Task 2) |
| `tests/test_continuation_detection.py` | Pending | Continuation tests (Task 3) |
| `tests/test_fuzzy_matching.py` | Pending | Fuzzy matching tests (Task 4) |
| `pyproject.toml` | Done | Added fuzzywuzzy |

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-23 | 0.1 | Story created | Morgan/Dex |

## Dev Agent Record

- **Agent:** Dex
- **Status:** Ready for development
- **Decision Log:** (to be populated during dev)
- **Completion Notes:** (to be populated on completion)
